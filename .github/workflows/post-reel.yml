name: Post Reel to X
# X API Free tierでは動画ツイートが403になるため自動実行を停止
# 動画はDriveからダウンロードして手動でXに投稿してください

on:
  workflow_dispatch:  # 手動実行のみ残す（自動スケジュールは無効）

jobs:
  post-reel:
    runs-on: ubuntu-latest
    if: false  # 無効化（手動でtrueに変えれば再度実行可能）
    steps:
      - run: echo "disabled"
